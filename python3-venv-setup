# To manage software packages for Python, install pip

sudo apt install -y python3-pip
pip3 intall package_name

# Install few more packages to make sure you have robust programming environment
sudo apt install build-essential libssl-dev libffi-dev python3-dev

# Setting up a virtual environment
sudo apt install -y python3-venv

mkldir environments
cd environments

python3.7 -m venv my_env

ls my_env

output:
bin include lib lib64 pyenv.cfg share

# Activate the environment
source my_env/bin/activate

Step 3 — Creating a “Hello, World” Program
